apiVersion: 1

providers:
  - name: "api-de-pedidos"
    ordId: 1
    folder: "API de Pedidos"
    folderUid: "api-de-pedidos"
    type: file
    disableDeletion: false
    allowUiUpdates: false
    updateIntervalSeconds: 30

    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false